{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"check:escapes": "node scripts/check-frontend-escapes.mjs",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.19.0",
		"@types/node": "^25.5.0",
		"@vitest/snapshot": "^4.1.0",
		"typescript": "^5.5.2",
		"vitest": "^4.1.0",
		"wrangler": "^4.115.0"
	},
	"dependencies": {
		"hono": "^4.12.9"
	}
}
